package com.mybus.jsonpath;

import org.springframework.stereotype.Service;

@Service
public class JSONMergeService {

/*	public JSONObject mergeJson(JSONObject source, JSONObject destination, String srcPath, String destPath){
		JSONObject sourceContact = JsonPath.read(source, sourcePath);
		JSONObject destContact = JsonPath.read(destination, destinationPath);
		for(Object key:destContact.keySet()){
			sourceContact.put(key, destContact.get(key));
		}
	}*/
	
}
